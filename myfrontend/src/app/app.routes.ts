import { Routes } from '@angular/router';
import { WoordenlijstComponent } from '../components/woordenschat/woordenlijst.component';

export const routes: Routes = [
    { path: '', component: WoordenlijstComponent },
];
